{
  "name": "vorg-eavy-site",
  "private": true,
  "type": "module",
  "scripts": {
    "build:algorithm": "tsc -p tsconfig.json",
    "test:algorithm": "npm run build:algorithm && node tests/drop-os-algorithm.test.mjs",
    "test:flow": "node test-drop-os-flow.mjs"
  },
  "devDependencies": {
    "playwright": "^1.60.0",
    "typescript": "^5.6.3"
  }
}
